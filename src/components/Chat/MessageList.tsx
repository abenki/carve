import React from 'react'

export default function MessageList(): React.ReactElement {
  return (
    <div className="flex-1 overflow-y-auto p-3 space-y-3">
      {/* Map over messages from useUIStore, render user + assistant bubbles */}
    </div>
  )
}
