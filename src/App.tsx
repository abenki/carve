import React from 'react'

export default function App(): React.ReactElement {
  return (
    <div className="flex flex-col h-full bg-neutral-900 text-white">
      {/* Toolbar */}
      {/* Main layout: thumbnails | viewport | chat */}
    </div>
  )
}
